export const Chips = () => (
    <div>Chips</div>
);

export const Filters = () => (
    <div>Filters</div>
);

export const Loader = () => (
    <div>Loader</div>
);

export const RequestExtract = () => (
    <div>RequestExtract</div>
);
